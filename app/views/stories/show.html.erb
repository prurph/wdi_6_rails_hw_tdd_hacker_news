<%= @story.title %> posted by <%= @story.user.username %>
<ul>
  <%= @story.comments.each do |comment| %>
    <li>
      <%= comment.body %><br>
      by <%= comment.user.username %>
    </li>
  <% end %>
</ul>
